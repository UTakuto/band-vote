import SubmissionForm from "@/components/SubmissionForm";

export default function page() {
    return (
        <div className="w-auto h-full flex justify-center items-center m-auto">
            <SubmissionForm />
        </div>
    );
}
